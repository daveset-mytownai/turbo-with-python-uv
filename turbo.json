{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [
        "prebuild",
        "^build"
      ],
      "outputs": [
        "output-file.txt",
        "dist/**"
      ]
    },
    "python-lib#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "uv.lock", "!__pycache__"]
    },
    "python-app#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "apps/python-app/uv.lock", "uv.lock", "!__pycache__"]
    },
    "python-app2#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "apps/python-app/uv.lock", "uv.lock", "!__pycache__"]
    },
    "prebuild": {},
    "lint": {},
    "check-types": {}
  }
}

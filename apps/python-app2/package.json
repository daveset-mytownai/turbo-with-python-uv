{
  "name": "python-app2",
  "version": "0.0.1",
  "scripts": {
    "dev": "uv run uvicorn main:app --reload",
    "build": "uv sync --link-mode=copy",
    "lint": "uv run ruff check --fix .",
    "fmt": "uv run ruff format .",
    "test": "uv run pytest"
  },
  "dependencies": {
    "@repo/python-lib": "workspace:*"
  }
}

